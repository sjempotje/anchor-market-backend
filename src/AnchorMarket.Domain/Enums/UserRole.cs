namespace AnchorMarket.Domain.Enums;

/// <summary>Defines the authorization level of a user.</summary>
public enum UserRole
{
    /// <summary>A standard user with regular permissions.</summary>
    User,
    /// <summary>An administrator with elevated permissions.</summary>
    Admin
}
