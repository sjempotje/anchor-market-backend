name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  release:
    types: [published]

jobs:
  build-amd64:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/*.fsproj', '**/packages.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Upload OpenAPI document
        uses: actions/upload-artifact@v4
        with:
          name: openapi-spec
          path: src/AnchorMarket.Api/obj/AnchorMarket.Api.json

      - name: Test
        run: dotnet test --no-build --configuration Release --verbosity normal --logger "trx;LogFileName=test-results.trx"

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-amd64
          path: |
            **/TestResults/*.trx

  build-arm64:
    runs-on: ubuntu-24.04-arm

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/*.fsproj', '**/packages.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Test
        run: dotnet test --no-build --configuration Release --verbosity normal --logger "trx;LogFileName=test-results.trx"

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-arm64
          path: |
            **/TestResults/*.trx

  tag:
    needs: [build-amd64, build-arm64]
    if: github.event_name == 'push' || github.event_name == 'release'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.handle_tag.outputs.version }}
      tag_created: ${{ steps.handle_tag.outputs.tag_created }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract version and create tag if missing
        id: handle_tag
        run: |
          VERSION=$(grep -oP '<Version>\K[^<]+' src/AnchorMarket.Api/AnchorMarket.Api.csproj)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          if [ "${{ github.event_name }}" = "push" ]; then
            if git tag --list | grep -qF "$VERSION"; then
              echo "tag_created=false" >> "$GITHUB_OUTPUT"
            else
              git tag "$VERSION"
              git push origin "$VERSION"
              echo "tag_created=true" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "tag_created=false" >> "$GITHUB_OUTPUT"
          fi

  publish:
    needs:
      - build-amd64
      - build-arm64
      - tag

    if: >
      github.event_name == 'release' ||
      (github.event_name == 'push' && needs.tag.outputs.tag_created == 'true')

    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          - runner: ubuntu-latest
            platform: linux/amd64
            suffix: amd64
          - runner: ubuntu-24.04-arm
            platform: linux/arm64
            suffix: arm64

    runs-on: ${{ matrix.runner }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: src/AnchorMarket.Api/Dockerfile
          push: true
          platforms: ${{ matrix.platform }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/anchormarket:${{ needs.tag.outputs.version }}-${{ matrix.suffix }}
            ghcr.io/${{ github.repository_owner }}/anchormarket:${{ github.sha }}-${{ matrix.suffix }}
          cache-from: type=gha,scope=${{ matrix.suffix }}
          cache-to: type=gha,mode=max,scope=${{ matrix.suffix }}

  merge:
    needs: [publish, tag]

    if: github.event_name == 'release' || needs.publish.result == 'success'

    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and push multi-arch manifest
        run: |
          docker buildx imagetools create \
            --tag ghcr.io/${{ github.repository_owner }}/anchormarket:${{ needs.tag.outputs.version }} \
            --tag ghcr.io/${{ github.repository_owner }}/anchormarket:latest \
            --tag ghcr.io/${{ github.repository_owner }}/anchormarket:${{ github.sha }} \
            ghcr.io/${{ github.repository_owner }}/anchormarket:${{ github.sha }}-amd64 \
            ghcr.io/${{ github.repository_owner }}/anchormarket:${{ github.sha }}-arm64

  release:
    needs: [build-amd64, build-arm64, tag]
    if: >
      github.event_name == 'release' ||
      (github.event_name == 'push' && needs.tag.outputs.tag_created == 'true')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download OpenAPI artifact
        uses: actions/download-artifact@v4
        with:
          name: openapi-spec
          path: openapi

      - name: Upload OpenAPI spec to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: openapi/AnchorMarket.Api.json
          update_release: true

      - name: Create release for new tag
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.tag.outputs.version }}
          files: openapi/AnchorMarket.Api.json
          generate_release_notes: true

  dispatch-sdks:
    needs: [release, tag]
    if: needs.release.result == 'success'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        sdk:
          - name: typescript
            repo: sjempotje/anchormarket-sdk-typescript
            generator: typescript
    steps:
      - name: Dispatch SDK generation
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.SDK_REPO_TOKEN }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: "${{ matrix.sdk.repo }}".split('/')[1],
              workflow_id: "generate-sdk.yml",
              ref: "main",
              inputs: {
                generator: "${{ matrix.sdk.generator }}",
                source_repo: "${{ github.repository }}",
                source_version: "${{ needs.tag.outputs.version }}"
              }
            });